<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Korlornium Map</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <style>
    #map { width: 100%; height: 500px; }
    body { margin: 0; }
  </style>
</head>
<body>
  <div id="map"></div>

  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script>
    const bounds = [[0, 0], [2100, 3000]];

    const map = L.map('map', {
      crs: L.CRS.Simple,
      minZoom: -3,
      maxZoom: 1,
      zoomSnap: 0.5,
      center: [1000, 2000],
      zoom: -2.5
    });

    const image = L.imageOverlay("Images/Korlornium.png", bounds).addTo(map);
    map.fitBounds(bounds);
    L.control.scale({ metric: true }).addTo(map);
  </script>
</body>
</html>
